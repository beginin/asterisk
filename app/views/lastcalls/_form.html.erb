<%= form_for(@lastcall) do |f| %>
  <% if @lastcall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lastcall.errors.count, "error") %> prohibited this lastcall from being saved:</h2>

      <ul>
      <% @lastcall.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :extention %><br>
    <%= f.text_field :extention %>
  </div>
  <div class="field">
    <%= f.label :tnumber %><br>
    <%= f.text_field :tnumber %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
